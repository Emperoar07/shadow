/** Faucet threshold: top up when margin balance falls below this (in USDC). */
export const FAUCET_TRIGGER_USDC = 10_000;

/** Faucet cap: top up to this target balance (in USDC). */
export const FAUCET_CAP_USDC = 20_000;

/** Token decimals for mUSDC. */
export const MUSDC_DECIMALS = 6;
