[build]
builder = "nixpacks"
buildCommand = "npm install && cd app && npm install && cd ../relay && npm install && npm run build"

[deploy]
startCommand = "node relay/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
