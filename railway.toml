[build]
builder = "nixpacks"
buildCommand = "cd relay && npm install && npm run build"

[deploy]
startCommand = "cd relay && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
